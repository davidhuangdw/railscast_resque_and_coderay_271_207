= simple_form_for(@snippet) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :language, collection: @snippet.all_languages
    = f.input :plain_code, input_html:{rows:'10'}
    -#= f.input :highlighted_code

  .form-actions
    = f.button :submit
